apiVersion: v1
kind: Service
metadata:
  name: vpromq01
spec:
  ports:

  - port: rmq
    targetPort: 5672
    protocol: TCP

  - port: rmqmg
    targetPort: 15672
    protocol: TCP

  selector:
    app: vpromq01
  type: ClusterIP
