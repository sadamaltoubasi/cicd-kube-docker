apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: ssm-backend
spec:
  provider:
    aws:
      service: ParameterStore
      region: us-east-1
#      auth:
  #      jwt:
    #      serviceAccountRef:
      #      name: eso-ssm-sa
        #    namespace: prod